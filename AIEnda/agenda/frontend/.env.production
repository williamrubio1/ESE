# En producción las llamadas /api/* van al mismo servidor (sin proxy)
# No hace falta REACT_APP_API_URL porque el proxy solo aplica en dev.
# Si en algún momento separas frontend y backend, define aquí la URL del backend:
# REACT_APP_API_URL=https://tudominio.com
